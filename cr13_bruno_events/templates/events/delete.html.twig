{% extends 'base.html.twig' %}

{% block title %}Delete an Event{% endblock %}

{% block body %}
<div class="jumbotron jumbotron-fluid mb-0" style="background-color: #8AADD7">
<h4 class="text-danger p-5">Are you sure you want to delete this event ?</h4>

	{{form_start(form)}}
        {{form_widget(form)}}<a href="{{ path('home') }}" class="btn btn-info btn-lg mb-2">Back to Main Menu</a>
        {{form_end(form)}}
</div>

{% endblock %}
